# Debug 文件和截图
debug/
debug/*.png
debug/on_error/*.png
*.jpg
*.jpeg

# 日志文件
*.log
go-service.log
maa*.log
mxu-*.log

# 大文件和编译产物
*.pdb
*.exe
*.dll
*.cache
cache/

# 系统文件
.DS_Store
Thumbs.db

# 用户配置（MXU 首次启动会自动生成；不要入仓，否则会冲掉每个用户的真实实例）
config/mxu-MaaBrownDust2.json

# 大模型文件（如果用 Git LFS 或单独下载）
resource/model/ocr/

# 运行时文件
*.log
*.pdb
*.tmp
config/mxu-MaaBrownDust.json

# Python 字节码缓存
__pycache__/
*.pyc

# 更新器运行时产物
updater_cache.json
updates/

# Agent 工作区记忆（不属于软件本身）
.workbuddy/

# 防御性忽略：这个 0 字节文件由旧版 launcher.bat 注释里的 ">" 重定向产生（不是 MXU 的编码 bug）。
# v26.09.7 起 launcher.bat 已改为纯 ASCII 且不含 ">"，源头已消除；规则保留，以防老目录里的残留被误提交。
鑚姩

# local, machine-specific files (shortcuts created by tools/apply_icon.ps1)
*.lnk
