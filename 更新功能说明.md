# BD2MAA 更新助手 / 启动器 使用说明

> 适用项目：`Maa-Assistant-Browndust2`（基于 MaaFramework + MXU）
> 新增/改动文件：`BD2MAA-Updater.ps1`、`launcher.bat`、`updater_config.json`
> 实现方式：**纯 PowerShell，零依赖，无需 Python**

本功能在**软件开启时自动检测 GitHub 仓库新版本**：
- 发现新版本 → 弹出**选择对话框**（含版本号与更新日志），由用户决定；
- 用户选「一键更新」→ **前台显示下载进度** → 下载 → **自动覆盖旧版本**；
- 用户选「暂不更新」→ **直接打开 `mxu.exe`** 进入软件。

---

## 一、怎么用（最重要）

> ⚠️ 以后请通过 **`launcher.bat`** 来打开软件，**不要**再直接双击 `mxu.exe`。
> 这样每次启动都会先检测一次更新。**首次启动时 launcher 会自动在根目录生成带图标的 `MaaBd2.lnk`**（指向 launcher.bat 与本项目的 `mxu.ico`），之后你可以双击它，或把它拖到桌面。

### 快捷方式与程序图标（推荐）
根目录的 `MaaBd2.lnk` 是**启动时自动生成**的（不是打包好的，避免不同用户机器路径不同导致 lnk 失效）：
- **首次启动**：launcher.bat 会自动创建 `MaaBd2.lnk`，目标 = `launcher.bat`，工作目录 = 项目根目录，图标 = `mxu.ico`
- **之后每次启动都会重建**（幂等、不耗资源），保证永远指向当前路径
- 想放到桌面：把 `MaaBd2.lnk` 拖到桌面即可；或右键 `launcher.bat` → 发送到桌面快捷方式，再手动改图标

#### 程序图标（`mxu.exe` / 启动器）
| 文件 | 作用 |
|------|------|
| `mxu.ico` | 图标源文件（多尺寸 16/24/32/48/64/128/256，已内嵌圆角） |
| `tools\apply_icon.ps1` | 图标写入 PowerShell 脚本（同时重建 `MaaBd2.lnk`）。高级用户可手动 `powershell -File tools\apply_icon.ps1` 调用，启动器每次启动也会自动按需调用 |

> MXU 自更新会替换 `mxu.exe`，图标可能变回默认。**`launcher.bat` 每次启动时会自动检测并按需补图标 + 重建 `MaaBd2.lnk`**，无需手动操作。

---

## 二、检测到新版本时的弹窗

| 按钮 | 作用 |
|------|------|
| **⬇ 一键更新** | 前台弹出下载进度窗口（带进度条与 MB/百分比），下载完成后**自动解压并覆盖旧版本**，然后启动软件 |
| **暂不更新** | 关闭弹窗，**直接启动 `mxu.exe`** 进入软件（下次启动仍会检测） |

> 说明：选择「一键更新」后，助手会**自动下载并覆盖**旧版本（无需你手动解压）。
> 更新时**会保留你 `config/` 目录下的已有配置**，仅补齐缺失的默认配置项，详见第五节。

### 20 秒无操作自动跳过（v26.09.7 新增）

弹窗内含倒计时提示「无操作 X 秒后自动跳过更新」：

- **20 秒内**点「⬇ 一键更新」或「暂不更新」→ 正常生效（计时器立即停）
- **20 秒内无任何点击** → 弹窗自动关闭、视作「暂不更新」、直接启动 `mxu.exe` 进入软件
- 设计动机：用户**无人值守**场景（挂机 / 远程启动 / 离开座位）不应被卡在更新窗口；定时自动跳过才能让「自动化日常」真正无人值守
- 倒计时只在发现新版本时出现；已是最新版本直接进软件，无弹窗无倒计时

---

## 三、更新失败时的手动兜底

正常流程下「一键更新」会自动完成覆盖，无需手动操作。若下载/解压异常，助手会提示你前往发布页手动下载：

1. 打开 `updates/` 文件夹，得到如 `MABd2vXX.XX.X.zip` 的压缩包（若已在下载目录）。
2. （可选）**备份 `F:\BD2MAA\config\mxu-MaaBrownDust2.json`**——这是你的任务实例与设置。注意：本发布 zip **根本不打包**这个文件，所以即使不备份也不会被覆盖（MXU 首次启动会自动生成默认实例；老用户已有的实例100% 保留）。
3. 把压缩包解压到 `F:\BD2MAA\` 并覆盖（推荐用 7-Zip / Bandizip 等“合并覆盖”模式；解压后根目录没有 `MaaBd2.lnk`，首次启动 launcher.bat 会自动重建）。
4. 重新通过 `launcher.bat` 进入软件。

---

## 四、命令行参数（高级）

在 `launcher.bat` 后面加 PowerShell 开关即可，例如 `launcher.bat -Demo`：

| 参数 | 作用 |
|------|------|
| `-Force` | 忽略缓存时间，强制重新请求 GitHub 检测 |
| `-Demo` | **演示弹窗**：即使本地已是最新也弹出更新窗口，用于预览效果 |
| `-Test` | 仅做逻辑自检（读取当前版本、拉取最新 release、选资源、比较版本），不弹窗、不启动软件 |
| `-Force` / `-Demo` / `-Test` | 这三个开关都会**忽略 6 小时缓存、强制联网检测**（普通启动在 6 小时内只检测一次，可能静默进游戏） |

> 当前你的本地 `interface.json` 版本为 `v26.09.3`，而 GitHub 最新发布为 `v26.09.2`，
> 所以**现在运行不会弹窗**（你本地比线上还新）。想先看弹窗效果请用 `-Demo`。

---

## 五、可配置项（`updater_config.json`）

| 字段 | 默认 | 说明 |
|------|------|------|
| `repo` | `alkaidjin/Maa-Assistant-Browndust2` | 检测的 GitHub 仓库 |
| `check_interval_hours` | `6` | 两次联网检测的间隔（避免触发 GitHub 60次/小时限流） |
| `asset_keywords_include` | `["MABd2","win","BD2","Browndust"]` | 优先匹配的压缩包关键字 |
| `asset_keywords_exclude` | 含 `source code`/`linux`/`macos` 等 | 排除的资源（如源码包、其他平台） |
| `protected_dirs` | `["config"]` | **更新时这些目录的"已有文件"不覆盖**（仅新增缺失的默认配置）。`config/mxu-MaaBrownDust2.json` 本身就不在 zip 里，参见下方"config/ 保留策略"小节 |
| `download_dir` | `updates` | 下载保存目录（相对项目根） |
| `log_retention_days` | `7` | `debug/` 下日志与调试截图的保留天数，超过即自动清除（见第八节） |
| `log_compact` | `true` | 启动时自动把 `debug/maa.log` / `maa.bak.log` 精简成可读的任务时间线（见第八节） |
| `log_compact_keep_recognition` | `false` | 精简时是否保留每一次识别的结果行。调识别参数时设 `true`，平时保持 `false` |
| `ensure_window_ready` | `true` | 启动器自动注入 `savedDevice.windowName` 并翻 `waitForExit=false`，让 MXU 内置的「等待窗口就绪」循环生效（详见第十节）。设 `false` 关闭整个自动注入 |

缓存文件 `updater_cache.json` 由程序自动维护，记录上次检测时间，无需手动改。

### 关于 `config/` 的保留策略（用户最关心）
更新覆盖时，`BD2MAA-Updater.ps1` 采用**双层保护**：

**第一层 · 发布 zip 根本不打包用户配置**
- `config/mxu-MaaBrownDust2.json`（每个用户的实例 + 任务 ID + enabled 状态 + optionValues）**不在 zip 里**——它本质是用户运行时产物，应该跟着机器走、不跟着版本走。
- 这意味着无论是**通过 launcher 走一键更新**、还是**手动解压覆盖**到 `F:\BD2MAA\`，这个文件都**永远不会被新版本覆盖**。
- 新用户首次启动 MXU 时，MXU 会自动按内置默认值生成一份 `mxu-MaaBrownDust2.json`，里面包含一份可用的默认任务实例。

**第二层 · 启动器的 `Copy-Update` 也会兜底**
- 对于 zip 里**确实存在**的 `config/` 文件（比如未来的某个新选项），`BD2MAA-Updater.ps1` 的 `Copy-Update` 函数带 `protected_dirs = ['config']`——已存在的文件保留，仅补齐缺失的默认项。

> 已用自动化测试验证：模拟更新中，既有用户配置 `config/user_existing.json` 保持不变，
> 而新默认项 `config/default_new.json` 被成功补齐，根目录 `mxu.exe`/`interface.json` 被更新。

### 关于覆盖的"原子性"与中途异常
- **大文件（≥ 1 MB，如 `mxu.exe`、模型 `.onnx`）** 走"先写 `<file>.new`、再 `Move-Item -Force` 改名覆盖"的两段式原子覆盖。
  中途断电 / 磁盘满 / 进程被杀时，旧文件不会变成半截，新文件留在 `*.new` 等下次启动清理。
- **小文件（json / png / txt）** 走直接 `Copy-Item -Force`——损坏也只是文本/配置损坏，重启会再被新版本覆盖回来。
- **下载失败**也会自动清理掉残留的半截 zip，`updates/` 不会越用越胖。

> 📌 **关于 `resource/`**：它存放的是软件自带的**任务流水线（`pipeline/`）、OCR 模型（`model/`）、识别图片（`image/`）与游戏数据（`gamedata/`）**，属于**软件资源而非用户配置**。每次更新都会**随新版本一并覆盖更新**——这正是预期行为：旧的任务定义 / 模型若不被更新，反而可能导致识别或执行出错。它不会被“保留”，也无需保留；真正需要保留的只有 `config/` 下的用户任务实例。

---

## 六、备选方案：启用 MXU 原生 GitHub 自动更新（在软件内）

MXU 本身已内置“GitHub 自动下载更新”能力，且你的 `interface.json` 已经写好：

```json
"github": "https://github.com/alkaidjin/Maa-Assistant-Browndust2",
"version": "v26.09.3"
```

如果你希望**完全在 MXU 界面内**完成更新（而不是本助手弹窗），可以：
- 在 MXU 设置中查找“检查更新 / 更新源”相关选项，确认更新源为 GitHub；
- 或保持现状使用上方“launcher.bat”的弹窗方案。

> ⚠️ 注意：MXU 原生更新可能会**整体覆盖项目目录**（包括你本地自定义的 `config/` 与 `resource/`），
> 使用前务必备份。本助手采用“下载后自动覆盖 + 保留 config/”的策略，更适合作者本人/重度自定义用户。

> 💡 关于 MirrorChyan：MXU 原生“检查更新”按钮依赖 `mirrorchyan_rid`（资源 ID）。
> 不接 MirrorChyan 也完全可以用本外置启动器做 GitHub 版本检测与更新，二者互不冲突。

---

## 七、常见问题

- **双击 .bat 一闪而过 / 没反应？** `launcher.bat` 现以纯 PowerShell 运行（无需 Python）。
  若仍异常，请右键 .bat「以管理员身份运行」或手动执行
  `powershell -NoProfile -ExecutionPolicy Bypass -File "BD2MAA-Updater.ps1"` 查看报错。
- **检测一直失败 / 没弹窗？** 检查网络是否能访问 `api.github.com`；GitHub 未登录限速 60 次/小时，
  频繁测试可用 `-Force` 但请节制。
- **我想恢复成直接打开 mxu.exe？** 直接双击 `mxu.exe` 即可，更新检测功能只是“额外的一层”，不影响软件本体。
- **更新会弄丢我的任务配置吗？** 不会。`config/mxu-MaaBrownDust2.json` **根本不在 zip 里**——新用户首次启动 MXU 自动生成默认实例，老用户的真实实例 100% 保留。即使你手动把 zip 解压覆盖到 `F:\BD2MAA` 也一样安全（详见第五节）。
- **根目录偶尔出现一个 0 字节的乱码文件（形如 `鍚?姩`）？** 那是 MXU 自身的运行时标记（UTF-8「启动」经系统 GBK
  代码页写盘的编码 bug），内容为空、无害、与更新功能无关。启动器会自动删除它；若被 MXU 的句柄占用而删不掉，
  则强制设置「隐藏 + 系统」属性，因此正常情况下你看不到；`.gitignore` 也已忽略该文件名，**不会进入 git 仓库**。无需干预。
- **点「启动任务」后 MXU 报「未搜索到任何窗口」秒退？** 详见第十节。启动器已经会自动处理，
  一般无需手动操作；如自动处理被关掉或想手动配，看下「连接设置」里手动选一次窗口即可恢复。

---

## 八、`debug/` 目录的自动精简与清理

`debug/` 是 MXU 与 MaaFramework 共同写日志的地方，长期运行会无限增长。它包含三类产物：

| 类型 | 示例 | 说明 |
|------|------|------|
| 日志 | `mxu-web-YYYY-MM-DD.log`、`mxu-agent.log`、`maa.log`、`go-service.log`、`mxu-tauri.log` | MXU / MaaFW 运行时输出 |
| 调试截图 | `StartGame_2026.09.05-09.34.43.115.png` 等 | `maa_option.json` 里 `save_on_error: true` 时，识别失败自动留存的现场截图 |
| 归档 | `archive/maa.log.20260911-192615.log.gz` | 精简前的原始日志（gzip，约为原始体积的 4%） |

### 8.1 第一步：精简 `maa.log`（`tools/compact_log.ps1`）

**为什么需要**：MaaFramework 的**文件**日志固定以 Trace 级别全量落盘，`config/maa_option.json` 里的
`stdout_level` **只管控制台，管不到文件**（这是上游的设计，没有配置项可以降低）。实测跑一轮日常任务就会产生
**9~16 MB / 2~5 万行**，其中约 99% 是事件通知、识别过程、主进程与 Agent 进程的重复投递。

**做了什么**：把"消防水管"式的原始日志压成一份能从头读到尾的「任务时间线」：

| 处理 | 内容 |
|------|------|
| 保留 | `WRN` / `ERR` / `FATAL`；任务边界；节点边界；`Task timeout` / `invalid node id`；`save on error` 路径；会话起止 |
| 丢弃 | 全部 `TRC` / `DBG` 行；`AgentClient::ctx_event_sink` 事件回显；`NextList.*` / `Recognition.*` / `Action.*` 事件 |
| 折叠 | 连续重复的同类警告（`Wrong ocr_result size` 一次能刷几百行 → 1 行 + `同类警告共 N 条，已折叠`） |
| 去重 | 同一事件被主进程 / Agent 进程重复投递的记录（时间差 ≤ 20ms） |
| 改写 | 事件行 → `<时间>  任务/节点  开始/完成/失败  <名字>` 单行形式 |

**实测效果**（一轮 12 任务的日常）：

```
原始    :  8,990,366 字节 / 27,905 行
精简后  :     52,396 字节 /    657 行
下降    : 行数 97.7%   体积 99.4%
```

精简后的事件时间线长这样，失败点一眼可见：

```
2026-09-11 18:14:41.261  任务    开始  StartPVP
2026-09-11 18:15:21.850    节点  开始  PVPBegin
2026-09-11 18:15:31.890    节点  开始  WaitForPVP
2026-09-11 18:16:02.655    节点  开始  Quit
2026-09-11 18:16:22.919  [WRN] Task timeout [pretask.name=Quit] [duration_since(start_clock)=20263ms]
2026-09-11 18:16:22.919    节点  失败 <<<  Quit
2026-09-11 18:16:22.952  [INF] save on error to F:/BD2MAA/debug/on_error/Quit_2026.09.11-18.16.22.920.png
2026-09-11 18:16:22.952  任务    失败 <<<  StartPVP
```

**安全设计**：

- 改写前先把原始日志 gzip 归档到 `debug/archive/`，**不会丢任何信息**（实测 8.9 MB → 385 KB，约 4%）；
- **按文件粒度**检测占用：MXU 正在写入的 `maa.log` 会被自动跳过，轮转出来的 `maa.bak.log` 照常精简。
  比"看进程在不在"更准，也因此即使开着 MXU 重新启动启动器也不会破坏文件；
- 完全幂等：对已精简的文件再跑一遍会直接报「无需处理」，不会重复归档。

### 8.2 第二步：按保留天数清理

**保留策略**：只保留最近 `log_retention_days` 天（默认 **7 天**），更早的一律删除（含 `archive/` 里的归档）。

启动器每次启动时都会自动执行「精简 + 清理」（见 `BD2MAA-Updater.ps1` 的 `Invoke-LogCleanup`）。
想手动执行或预览：

```bat
:: 先看能压掉多少，不动文件
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1" -DryRun

:: 真跑一次精简
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1"

:: 正在调识别参数：连每次识别的结果行一起保留
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1" -IncludeRecognition

:: 只清理过期文件（不做精简）
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\clean_logs.ps1" -Days 7
```

> 只想改策略、不想敲命令行？直接改 `updater_config.json` 里的 `log_retention_days` / `log_compact` 即可，启动器会读它。
> 以上操作**只作用于 `debug/`**，不会碰 `config/`、`resource/`、`tasks/` 或任何其他目录。

---

## 九、加速下载：自动镜像源链（v26.09.4 新增）

### 为什么需要

GitHub Releases 在国内普遍被严重限速（实测最低 10~30 KB/s，下一个 60 MB 的 zip 可能要 30 分钟以上）。
更换下载器（`BITS` / `aria2` / `curl`）都无效——**限速在源站，不在客户端**。

### 解决方案：updater 内置「镜像前缀重试链」

启动器 `BD2MAA-Updater.ps1` 现在的下载流程：

```
1. 取配置的 mirror_url_prefixes 列表（默认 4 个国内常见 GitHub 镜像）
2. 把每条镜像前缀拼到 GitHub 原 URL 前面，得到一组候选 URL
3. 顺序下载，第一个成功的就用，剩下的跳过
4. 全部失败 → 最后回退到 GitHub 原 URL（终极兜底）
```

下载窗会显示当前正在用的源（例如 `源：ghfast.top`），方便你看哪个镜像在工作。

### 默认镜像列表（`$DEFAULTS` / `updater_config.json`）

```json
"mirror_url_prefixes": [
    "https://ghps.cc",
    "https://ghfast.top",
    "https://gh-proxy.com",
    "https://mirror.ghproxy.com"
]
```

> 这份列表本身不带 `https://`，会自动补；用户也可以直接写 host（例如 `cdn.your-corp.com`，会自动补 `https://`）。

### 配置项：`updater_config.json → mirror_url_prefixes`

| 动作 | 怎么改 |
|---|---|
| **关闭镜像，只用官方** | `"mirror_url_prefixes": []`（实测海外/海外代理场景可加速首次检测） |
| **追加公司/家用代理** | 在数组里加自己的条目，例如 `"https://your-proxy.example.com/gh"` |
| **替换默认的 4 个** | 直接整个覆盖数组。格式：每条是「主机名」或「完整 URL 前缀」 |
| **优先级** | 数组里**靠前**的先试。如果常用的 `ghfast.top` 不稳，把它删掉或移到末尾 |

### 怎么找到你环境能用的镜像

**没时间试错？最快路径**：

1. 浏览器直接打开下面 4 个 host（例如 `https://ghfast.top/`），哪个能进就是你能用的；
2. 把不能访问的删掉，能访问的提到前面；
3. 重启 `launcher.bat`，下次更新就能从你配置的镜像下。

**自带工具**：v26.09.4 起，下载窗会在「源」一行显示当前用的镜像名。下载卡住时可以按 `Ctrl+C` 关掉窗口，updater 自动切下一个候选源；全部失败的话会弹窗让你去 release 页手动下载。

### 常见问题

- **「源」显示 `ghps.cc` 但下载还是慢？** 该镜像当前可能临时限速。把它挪到数组末尾，下次先试别的。如果全部都慢，先确认你的本地网络到这些镜像的连通性（不是 GitHub 的连通性）。
- **公司代理怎么配？** 比如公司搭了 `http://proxy.local:7890` 的代理并对外暴露 GitHub 镜像。把它写成 `"http://proxy.local:7890/https://github.com"` 这种形式加到数组最前面。
- **关闭镜像反而比开镜像快？** 那大概率你走海外网络。在配置里写 `"mirror_url_prefixes": []` 即可只走官方。

---

## 十、自动解决「未搜索到任何窗口」秒退（v26.09.6 新增）

### 问题现象

在 MXU 里点「启动任务」，前置程序（或你手动）已经拉起了游戏 exe，但 MXU 紧接着报：

```
[MAA] 搜索 Win32 窗口, classRegex: UnityWndClass , windowRegex: BrownDust II
[MAA] 找到 Win32 窗口: 0 个
[Task] 实例 配置 1: 未搜索到任何窗口
[Task] 任务启动异常: {}
```

任务被秒退，新用户每次都卡在这步——直觉是「检测太快、窗口没拉起」，于是重试、重启、换 preAction……但始终无解。

### 根因（不是检测太快）

MXU 内部**确实有**一个 300 次 × 1 秒（共 5 分钟）的「等待窗口就绪」循环。但它被一条守卫卡住了：

```ts
if (!(waitForExit ?? true) && savedDevice && controller) {
  // 只有这里才进入等待循环
} else if ((Win32 || Gamepad) && savedDevice.windowName) {
  // 仅当 savedDevice.windowName 已设置时才比对待查找的窗口
} else {
  // 否则 deviceFound = true，立即跳过等待——秒退
}
```

新用户从来没在 MXU 「连接设置」里手动选过窗口，`savedDevice` 字段根本不在 config 里，于是守卫短路、走"一次性搜索"分支——这一次搜不到就直接 return false。

### 启动器的自动处理

`BD2MAA-Updater.ps1` 在拉起 `mxu.exe` 之前，会自动给 `config/mxu-MaaBrownDust2.json` 里的 Win32/Gamepad 实例注入两件事：

| 注入 | 默认值 | 作用 |
|---|---|---|
| `savedDevice.windowName` | `"BrownDust II"` | 解锁 MXU 等待循环里的 `savedDevice.windowName` 分支 |
| `preAction.waitForExit` | `false` | MXU 守卫要求 `waitForExit=false` 才进入等待；不翻就跳过 |

注入完全**幂等**：已经正确的不动，用户手动设过的窗口名也不动。try/catch 全程静默——config 损坏也不阻断启动。

### 不会动的场景（避免误伤）

| 场景 | 行为 |
|---|---|
| `controllerName` 不是 Win32/Gamepad（如 Adb / Mac） | 整个实例**不动** |
| `preAction.enabled = false` | 不动 |
| `preAction.program` 看起来**不是游戏 exe**（路径不含 `browndust`，大小写不敏感） | `waitForExit` **不动**（保留你精心配的"等这个工具跑完再继续"语义） |
| `savedDevice.windowName` 用户已自定义 | **不覆盖** |
| 配置损坏 / 解析失败 | 静默跳过，不阻断启动 |

> 启发式 `program` 含 `browndust` 才会翻 `waitForExit`：覆盖 `BrownDust II.exe`（默认安装）、
> `browndust-launcher.exe`（自定义启动器通常会命名含 BrownDust）等；**不会**翻
> `setup.bat`、`LDPlayer.exe`、各种 dev 工具——这些场景你的 preAction 通常是"等它跑完再继续"的语义，
> 翻成 false 会破坏意图。

### 关闭自动注入

`updater_config.json` 里加：

```json
"ensure_window_ready": false
```

关闭后整个 `Ensure-WindowReady` 函数 no-op——你想要的"我自己手动配"场景生效。重新打开只需把值改回 `true` 或删掉这一行。

### 兜底工具（仍然保留）

`tools/启动游戏并等待窗口.bat` + `tools/wait_game_window.ps1` 这套独立的"窗口就绪适配器"仍然在仓库里，针对以下场景：

- 自定义安装路径（非默认 `C:\Neowiz\Browndust2\Browndust2_10000001\`）
- 想用更激进的窗口就绪校验
- 想完全自己控制 preAction 的等待时机

用法：在 MXU 「前置程序」配置里把 program 指向 `tools/启动游戏并等待窗口.bat`，**args 留空**，「等待退出」开 ON。
`.bat` 必须**纯 ASCII**，长说明放 `.ps1`（UTF-8 with BOM），否则 cmd 按 GBK 误解析会刷乱码 stderr（脚本仍能跑，但日志噪声大）。

### 验证

测试覆盖 8 + 6 = 14 个用例，全部 PASS（详见 `cache/_test_v2_out.txt`）：

| 类别 | 用例 |
|---|---|
| 基础 | 全新用户注入、幂等性、用户自定义窗口名保留、空 `{}` 补 windowName |
| 兼容性 | Adb 实例跳过、Gamepad 实例注入、真实 config 路径端到端、已正确 no-op |
| **新约束** | 游戏 exe → 翻 waitForExit；setup.bat / 模拟器 → **不动** waitForExit；自定义 BD2 启动器 → 翻；`ensure_window_ready=false` → 整体 no-op |

> ⚠️ **本节已被 [§ 十一、底层升级](更新功能说明.md#十一底层升级mxu-v1131--v253--maaframework-v57--v13v26097-新增) 取代（v26.09.7+）**。
> 升级到 MXU v2.5.3 + MaaFramework v5.13.0 后，MXU 上游已经原生支持「等待窗口就绪」循环无 `savedDevice` 门槛 + `preActionConnectDelaySec` 稳定延迟，`Ensure-WindowReady` 函数已被移除、`updater_config.json` 的 `ensure_window_ready` 字段已删除、`tools/wait_game_window.ps1` 与 `启动游戏并等待窗口.bat` 已清理。
> 本节保留作为 v1.13.1 → v2.5.3 之间过渡版本的兼容补丁历史，便于老用户回看。

---

## 十一、底层升级：MXU v1.13.1 → v2.5.3 + MaaFramework v5.7 → v5.13（v26.09.7 新增）

> 适用版本：本次（v26.09.7）版本起，仓库随包发布的 `mxu.exe` 锁定 **v2.5.3**，`maafw/*` 锁定 **v5.13.0**。
> 备份与回滚：升级前的 `maafw/`（21 个文件，73.5 MB）、`mxu.exe`（21.4 MB）、`config/mxu-MaaBrownDust2.json` 完整保留在 `cache/backup_v26.09.6/`。任何时候 `Move-Item` 即可回滚。

### 升级前 vs 升级后

| 组件 | 升级前 | 升级后 |
|---|---|---|
| `mxu.exe`（GUI + Agent 主控） | v1.13.1（2026-02-11）| v2.5.3（2026-09-10） |
| `maafw/MaaFramework.dll` | 2.55 MB | 3.80 MB |
| `maafw/MaaWin32ControlUnit.dll` | 740 KB | 1.68 MB |
| `maafw/MaaToolkit.dll` | 559 KB | 1.05 MB |
| `maafw/MaaNode.node` | 1.06 MB | 1.57 MB |
| `maafw/MaaAgent{Client,Server}.dll` | 1.23 + 1.39 MB | 2.34 + 2.48 MB |
| `maafw/onnxruntime_maa.dll` | 19.0 MB | 19.2 MB |
| `maafw/DirectML.dll`、`ViGEmClient.dll` | 跳过（hash identical） | — |

总 DLL 集合：17 个文件全部替换，2 个外部依赖（DirectML、ViGEmClient）保持不变。

### 解决了哪些问题

1. **任务启动失败：`Tasker not properly initialized`**（v2.5.x 已修复）
   - v1.13.1：MXU 的 `tasker handle` 复用死状态——你手动停止一次任务后，下次「开始任务」立刻挂
   - 修复：v2.5.3 `start_tasks_impl` 在 `if !tasker.inited()` 时**自动丢弃并重建**，不再复用死 tasker
   - 代码定位：`MXU/src-tauri/src/commands/maa_agent.rs` `let needs_new_tasker = match instance.tasker.as_ref() { None => true, Some(t) => !t.inited() }`

2. **`savedDevice.windowName` 强制门槛**（v2.5.x 已移除）
   - v1.13.1：新用户首次启动，MXU 的 5 分钟等待循环被 `savedDevice.windowName` 守卫短路，落到「自动搜索设备」分支做一次性搜索 → `未搜索到任何窗口` 秒退
   - 修复：v2.5.3 守卫改为 `shouldWaitAfterPreActions = !!controller`（只看 controller 是否存在），不再依赖 savedDevice

3. **`preAction.args` 引号解析**（v2.5.x 改用 shell-words 分词）
   - v1.13.1：`split_whitespace()` 不识别引号，路径含空格必爆
   - 修复：v2.5.3 用 `shell_words::split`，原生支持 `"C:\Program Files\..."` 这类带空格参数

4. **`useCmd` / `shell_words::split`**（v2.5.x 新增）
   - 上一条带来的副作用：现在可显式选择 `cmd /c` 走 .bat，避免 PowerShell 误调用 .ps1 的问题

5. **窗口稳定延迟 `preActionConnectDelaySec`**（v2.5.x 新增）
   - v1.13.1：窗口找到后立刻连接，游戏仍在加载动画 → 偶发连接错误
   - 修复：v2.5.3 默认 `preActionConnectDelaySec = 5` 秒，找到窗口后再等 5 秒沉降再连接

6. **minimaafw 检查**（v2.5.3 强制）
   - v2.5.x 启动时校验 `MIN_MAAFW_VERSION = "5.12.1"`；旧版 maafw 启动即报错。该机制保护用户不会误用严重不兼容的 maafw

### 升级后回退了哪些之前打的补丁

升级前我们为了对付 v1.13.1 的 bug 打了 3 处补丁，升级到 v2.5.3 后这 3 处补丁**已无意义，回退/移除**：

| 补丁文件 | 移除原因 | v2.5.3 替代机制 |
|---|---|---|
| `BD2MAA-Updater.ps1` → `Ensure-WindowReady` 函数 | v2.5.3 不再需要 savedDevice 注入（守卫已放宽） | `shouldWaitAfterPreActions = !!controller`，原生效 |
| `tools/wait_game_window.ps1` | v2.5.3 原生 300×1s 等待循环 + 5s 沉降延迟 | Window ready 兜底已内置 |
| `tools/启动游戏并等待窗口.bat` | 同上 | — |
| `updater_config.json` → `ensure_window_ready: true` | 总开关不再需要（注入函数已删） | — |
| `更新功能说明.md` 第 十 节 | 升级后默认行为已符合预期 | 该节保留作为「曾发生的兼容补丁」历史记账 |

**回退顺序**：移除补丁代码 → 移除兜底工具 → 清理配置文件 → 跑一次启动器校验（看 log 无 `Ensure-WindowReady` 调用记录、无兜底 .bat 启动记录）。`update/启动游戏并等待窗口.bat` 如果你还有用，**暂不删**，作为「自定义安装路径」等边缘场景的手动手段；`Ensure-WindowReady` 的 `updates/mxu-MaaBrownDust2.json` 注入逻辑已被 MXU 上游替代。

### 升级操作步骤（参考）

```text
1. 备份：把整个 maafw/ + mxu.exe + config/ 下实例配置 备份到 cache/backup_v<old-ver>/
2. 下载 MXU-<platform>-<arch>-v2.5.3.zip + MAA-<platform>-<arch>-v5.13.0.zip
3. 取 MXU zip 里的 mxu.exe 单文件；取 MaaFramework zip 里 bin/* + share/MaaAgentBinary/*
4. 覆盖：mxu.exe 替换；maafw/* 全套替换；resource/model/ocr/*.onnx 保持不动
5. 首次启动时 MXU 自动迁移 config schema（如新增 preActionConnectDelaySec 默认 5 秒）
6. 跑一次 launcher.bat，看 mxu-web 日志确认无「Tasker not properly initialized」、「未搜索到任何窗口」
7. （可选）回滚：把 backup 目录拷回即可（不需要重装）
```

### 第三方致谢

本次升级后 BD2MAA 链接了 LGPL-3.0 的 **MaaXYZ/MaaFramework**，因此在仓库根创建了独立的 [NOTICE.md](NOTICE.md)：

- MaaFramework（LGPL-3.0）「重新链接」履行说明
- MXU（AGPL-3.0）「Combined Works / Remote Network Interaction」履行说明
- MaaEnd（AGPL-3.0）fork 起点与合规继承
- PaddleOCR（Apache-2.0）OCR 模型来源

### 仍未解决 / 已知遗留

- `debug/maa.bak.log` 单文件 16.7 MB（升级前就在，未过期故未被清理）
- `.gitignore` 部分中文注释显示为乱码（编码问题，不影响功能，与升级无关）
- `interface.json` 保留 PlayCover 入口，但所有 task 限定 Win32 → macOS 用户会困惑
- pipeline 长链缺统一容错（OCR 失败靠 fallthrough 到 StartGame）—— 与底层版本**无关**，原本就在 backlog 上

### 没动的东西

- `interface.json` schema（`interface_version: 2` 是上游稳定兼容的）
- `tasks/*.json` / `resource/pipeline/*.json`（你的 13 个任务 + 13 个 pipeline 全部 drop-in 兼容，扫描 100% 是内置 OCR/TemplateMatch/Click/Swipe 节点，零 custom_xxx 节点）
- `resource/model/ocr/*.onnx`（BD2MAA 自带的 OCR 模型与新 runtime 兼容）
- `agent/go-service` / `interface.json` 的 agent 配置
- `config/mxu-MaaBrownDust2.json` 你的真实实例（保留你之前手动选过窗口的状态）
